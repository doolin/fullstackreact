// Functional Component Style
// From r40, p. 185: When using a function component,
// the function itself is essentially the rnder() function.
const FunctionComponent = props => {
  return <div />;
};

export default FunctionComponent;
